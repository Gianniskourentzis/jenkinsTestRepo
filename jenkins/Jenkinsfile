pipeline {
    agent any 

    stages {
        stage('Checkout') {
            steps { 
                git 'https://github.com/Gianniskourentzis/jenkinsTestRepo.git'
            }
        }
        
        stage('Build and Run with Docker Compose') {
            steps {
                script { 
                    sh 'docker compose pull '
                    sh 'docker compose up -d '
                }
            }
        }
    }

    
}
